project_id                 = "your-gcp-project-id"
region                     = "us-central1"
zone                       = "us-central1-a"
supabase_access_token      = "your-supabase-access-token"
supabase_organization_id   = "your-supabase-organization-id"
spring_datasource_password = "your-supabase-db-password"
telegram_bot_token         = "your-ahun-telegram-bot-token"

# Per-service values (chat id, Cloud Run env) now live in the service/ root and
# are set by each service's pipeline — not here.

